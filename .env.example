# =============================================================================
# Family Budget Service - Environment Configuration
# =============================================================================
# Copy this file to .env and customize for your environment

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# -----------------------------------------------------------------------------
# Database Configuration (SQLite)
# -----------------------------------------------------------------------------
# Path to SQLite database file
# For Docker: /data/budget.db (mounted as volume)
# For local dev: ./data/budget.db
DATABASE_PATH=/data/budget.db

# -----------------------------------------------------------------------------
# Reverse proxy
# -----------------------------------------------------------------------------
# CIDR list (comma-separated) of proxies whose X-Forwarded-For is trusted by the
# login rate limiter. Empty = trust only the TCP peer address.
# TRUSTED_PROXIES=

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Environment: development | production
ENVIRONMENT=production

# Logging Configuration
# Levels: debug | info | warn | error
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Docker Compose Configuration
# -----------------------------------------------------------------------------
# Directory for SQLite data (used by docker-compose)
# Compose bind-mounts $DATA_DIR/backups into /backups — the directory must exist before `up`
DATA_DIR=./data

# Where backups are written. Empty means <dir(DATABASE_PATH)>/backups;
# compose sets /backups so copies do not land inside the database volume.
# BACKUP_DIR=

# NOTE: compose v2 reads `.env` from the *project directory*, which defaults to the
# directory of the first `-f` file (i.e. `docker/`). Keep this file in the repo root
# and pass `--project-directory .`, exactly as the `make docker-*` targets do:
#   docker compose --project-directory . -f docker/docker-compose.yml up -d
