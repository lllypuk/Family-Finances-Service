# Текущие задачи проекта Family Finances Service

## Статус выполнения

**Завершено:** Задачи 1-4 (67% от общего объёма)
- ✅ Задача 1: Удаление FamilyID из веб-слоя
- ✅ Задача 2: Удаление ReadinessHandler и LivenessHandler
- ✅ Задача 3: Обновление веб-тестов
- ✅ Задача 4: Onboarding Wizard (был реализован ранее)

**В работе:** Задача 5 (20% готово)
**Ожидает:** Задача 6

---

## Активные задачи для выполнения

---

### Задача 5: Реализовать управление пользователями через invite (Фаза 4.2)

**Приоритет:** Высокий (следующая в очереди)
**Статус:** В работе (20% готово)

**Описание:**
Admin должен иметь возможность добавлять и удалять участников семьи через веб-интерфейс. Открытая регистрация отключена — новые пользователи добавляются только через invite от admin.

**Уже выполнено:**
- ✅ Доменная модель `Invite` в `internal/domain/user/invite.go`
- ✅ Интерфейс `InviteRepository`
- ✅ Методы валидации (IsValid, IsExpired, Accept, Revoke)
- ✅ Генератор криптостойких токенов

**Шаги для завершения:**

1. **Создать SQLite репозиторий:**
   - `internal/infrastructure/user/invite_repository_sqlite.go`
   - Реализовать все методы интерфейса `InviteRepository`
   - Добавить тесты для репозитория

2. **Создать миграцию БД:**
   - Файл в `migrations/` для таблицы `invites`
   - Поля: id, family_id, created_by, email, role, token, status, expires_at, accepted_at, accepted_by, created_at, updated_at
   - Индексы: token (уникальный), family_id, email, status

3. **Создать сервис управления invite:**
   - `internal/services/invite_service.go`
   - Методы: CreateInvite, GetInviteByToken, AcceptInvite, RevokeInvite, ListFamilyInvites
   - Валидация прав доступа (только admin может создавать/удалять)

4. **Страница управления пользователями (admin only):**
   - Хэндлер `internal/web/handlers/admin.go` с методами:
     - `ListUsers()` - список пользователей семьи
     - `CreateInvite()` - создание invite
     - `DeleteUser()` - удаление пользователя
   - Шаблон `internal/web/templates/admin/users.html`
   - HTMX для динамического управления списком

5. **Страницы регистрации по invite:**
   - Хэндлер методы в `internal/web/handlers/auth.go`:
     - `InviteRegisterPage()` - форма регистрации
     - `InviteRegister()` - обработка регистрации
   - Шаблон `internal/web/templates/invite.html`
   - Валидация токена и срока действия

6. **Endpoint'ы (регистрация маршрутов):**
   - `GET /admin/users` — список пользователей
   - `POST /admin/users/invite` — создать invite
   - `DELETE /admin/users/:id` — удалить пользователя
   - `GET /invite/:token` — форма регистрации по invite
   - `POST /invite/:token` — регистрация по invite

7. **Тесты:**
   - Unit-тесты для сервиса
   - Integration-тесты для репозитория
   - Тесты хэндлеров

8. **Проверить:**
   - `make build` — компиляция
   - `make test` — все тесты проходят
   - `make lint` — 0 ошибок линтера

**Файлы:**
- ✅ `internal/domain/user/invite.go` (создан)
- ⏳ `internal/infrastructure/user/invite_repository_sqlite.go` (нужно создать)
- ⏳ `internal/services/invite_service.go` (нужно создать)
- ⏳ `internal/web/handlers/admin.go` (нужно создать)
- ⏳ `internal/web/templates/admin/users.html` (нужно создать)
- ⏳ `internal/web/templates/pages/invite.html` (нужно создать)
- ⏳ `migrations/XXX_create_invites_table.sql` (нужно создать)
- ⏳ `internal/web/web.go` — добавить регистрацию маршрутов

---

### Задача 6: Реализовать бэкап/восстановление через интерфейс (Фаза 4.3)

**Приоритет:** Средний
**Статус:** Ожидает выполнения (после Задачи 5)

**Описание:**
Добавить возможность скачивать и загружать файл БД (SQLite) через веб-интерфейс для резервного копирования и восстановления данных.

**Шаги:**

1. **Endpoint для скачивания бэкапа:**
   - `GET /admin/backup/download` — скачать файл БД
   - Использовать SQLite Online Backup API (или просто копию файла)
   - Content-Disposition: attachment с именем вида `family-budget-YYYY-MM-DD.db`
   - Доступ только для admin

2. **Endpoint для восстановления:**
   - `POST /admin/backup/restore` — загрузить файл БД
   - Валидация загруженного файла (проверить, что это SQLite БД)
   - Заменить текущую БД загруженным файлом
   - Перезапустить соединения с БД
   - Доступ только для admin

3. **Страница управления бэкапами:**
   - `GET /admin/backup` — UI для скачивания/загрузки
   - Показать размер текущей БД
   - Показать дату последнего бэкапа (если хранить)
   - Кнопка "Скачать бэкап"
   - Форма загрузки файла для восстановления с подтверждением

4. **Безопасность:**
   - Валидация MIME-типа и заголовка SQLite
   - Проверка целостности БД перед заменой
   - Автоматический бэкап текущей БД перед восстановлением
   - Rollback в случае ошибки

5. **Проверить:**
   - `make build`, `make test`, `make lint`
   - Ручное тестирование: скачать → удалить данные → восстановить → проверить данные

**Файлы:**
- `internal/web/handlers/backup.go` (новый)
- `internal/web/templates/admin/backup.html` (новый)
- `internal/services/backup_service.go` (новый, опционально)
- `internal/application/http_server.go` — регистрация маршрутов

---

## Порядок выполнения

```
✅ Задача 1 (FamilyID из веб-слоя) — ВЫПОЛНЕНА
    │
    ├──→ ✅ Задача 3 (веб-тесты) — ВЫПОЛНЕНА
    │
✅ Задача 2 (Readiness/Liveness) — ВЫПОЛНЕНА
    │
✅ Задача 4 (Onboarding wizard) — ВЫПОЛНЕНА
    │
    └──→ 🚧 Задача 5 (Invite-система) — В РАБОТЕ (20%)
         │
         └──→ ⏳ Задача 6 (Бэкапы) — ОЖИДАЕТ
```

## Критерии завершения каждой задачи

- `make build` — проект компилируется без ошибок
- `make test` — все тесты проходят
- `make lint` — 0 ошибок линтера
- Код соответствует существующим паттернам проекта (Clean Architecture, HTMX + PicoCSS)

## Замечания

- Задачи 1-4 полностью выполнены и протестированы
- Для Задачи 5 уже создана базовая доменная модель, требуется доработка инфраструктурного слоя
- Задача 6 независима от Задачи 5, но рекомендуется выполнять последовательно
