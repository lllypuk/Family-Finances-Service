# Бэклог задач для проекта "Family Finances Service"

## Контекст

Проект переходит на модель **self-hosted решения для одной семьи**: один Docker-образ = один экземпляр = одна семья. Необходимо убрать мультитенантность (family_id как параметр фильтрации во всех слоях), упростить observability и привести код в соответствие с новой архитектурой.

---

## Фаза 1: Убрать мультитенантность (family_id)

Сейчас `familyID` используется как ключ изоляции данных во всех слоях: домен, репозитории, сервисы, хэндлеры, веб-middleware. В self-hosted модели один экземпляр обслуживает одну семью, поэтому family создается при первом запуске и привязывается ко всем данным автоматически.

### 1.1 Ввести концепцию "единственной семьи" ✅ ВЫПОЛНЕНО
- [x] Добавить автоматическое создание семьи при первом запуске (seed/bootstrap)
- [x] Хранить ID семьи в конфигурации или получать из БД (единственная запись в таблице `families`)
- [x] Убрать API endpoint'ы создания/удаления/редактирования семей
- [x] Убрать `FamilyRepository` из интерфейсов (или свести к `GetFamily()`)

**Выполнено в коммите:** `5499c8e` (32 файла)

### 1.2 Упростить интерфейсы репозиториев ✅ ВЫПОЛНЕНО
- [x] Убрать параметр `familyID` из всех методов репозиториев
- [x] `GetByFamilyID(ctx, familyID)` → `GetAll(ctx)` или аналог
- [x] `Delete(ctx, id, familyID)` → `Delete(ctx, id)`
- [x] `GetTotalByFamilyAndDateRange(ctx, familyID, ...)` → `GetTotalByDateRange(ctx, ...)`
- [x] `GetByFamilyAndCategory(ctx, familyID, ...)` → `GetByCategory(ctx, ...)`
- [x] `GetActiveBudgets(ctx, familyID)` → `GetActiveBudgets(ctx)`
- [x] `GetByPeriod(ctx, familyID, ...)` → `GetByPeriod(ctx, ...)`

**Детали:**
- Добавлены helper-методы `getSingleFamilyID()` во все репозитории
- Обновлены все методы Create/Update/Delete для автоматического получения familyID
- Переименованы методы GetByFamilyID → GetAll во всех репозиториях

### 1.3 Упростить сервисный слой ✅ ВЫПОЛНЕНО
- [x] Убрать параметр `familyID` из методов BudgetService
- [x] Обновить `internal/services/interfaces.go` для BudgetService
- [x] Обновить `internal/services/budget_service.go`
- [x] Обновить `internal/services/category_service.go`
- [x] Методы вида `GetTransactionsByFamily(ctx, familyID, ...)` → `GetTransactions(ctx, ...)`
- [x] `CreateDefaultCategories(familyID)` → выполняется при bootstrap
- [x] `GetCategoryHierarchy(familyID)` → `GetCategoryHierarchy()`

**Детали:**
- Все сервисные интерфейсы в `internal/services/interfaces.go` очищены от familyID
- Все реализации сервисов обновлены

### 1.4 Упростить API хэндлеры ✅ ВЫПОЛНЕНО
- [x] Убрать `family_id` как query parameter из всех endpoint'ов
- [x] Убрать `FamilyID` из request-типов (`CreateTransactionRequest`, `CreateCategoryRequest`, `CreateUserRequest`)
- [x] Убрать валидацию `family_id` из хэндлеров

**Детали:**
- В `internal/application/handlers/types.go` нет FamilyID в request-типах
- Хэндлеры не используют family_id как query parameter

### 1.5 Упростить веб-слой 🔄 ПОЧТИ ВЫПОЛНЕНО
- [x] Обновить веб-хэндлеры (dashboard, transactions, categories, budgets, reports) — не читать `sessionData.FamilyID`
- [ ] Убрать `FamilyID` из `SessionData` (`internal/web/middleware/session.go` — строка 29)
- [ ] Убрать `SessionFamilyKey = "family_id"` из констант сессии (`internal/web/middleware/session.go` — строка 19)
- [ ] Убрать `FamilyID` из `SessionData` в `internal/web/models.go` (строка 14)
- [ ] Обновить тест `internal/web/models_test.go` — убрать FamilyID из тестовых данных

**Статус:** FamilyID объявлен в структурах, но нигде не используется в рантайме. Веб-хэндлеры уже не читают `sessionData.FamilyID`. Осталось удалить неиспользуемые поля и константы.

### 1.6 Упростить доменные модели ✅ ВЫПОЛНЕНО
- [x] Убрать поле `FamilyID` из доменных сущностей
- [x] Убрать метод `GetFamilyID()` у `Transaction`, `Budget`, `Report`
- [x] Упростить валидацию — не требовать `FamilyID` при создании сущностей
- [x] Обновить конструкторы `NewTransaction()`, `NewCategory()`, `NewBudget()`, `NewReport()`, `NewUser()`
- [x] Обновить все тесты доменных моделей

**Детали:**
- Поле FamilyID удалено из всех 5 доменных сущностей
- Методы GetFamilyID() удалены (Transaction, Budget, Report)
- Конструкторы обновлены — не принимают familyID
- Тесты обновлены (5 файлов: *_test.go)
- DTO мапперы частично обновлены (uuid.Nil для обратной совместимости API)

### 1.7 Упростить миграции БД ⚠️ РЕШЕНО ПО ДИЗАЙНУ
- [x] Оставить `family_id` в таблицах для совместимости, не требовать в бизнес-логике
- [x] Единственная запись family создаётся при bootstrap (Фаза 1.1)

**Статус:** Колонки `family_id` остаются в SQL-схеме. Репозитории автоматически получают familyID через `getSingleFamilyID()`. Это сознательное решение — бизнес-логика не зависит от family_id, но данные в БД остаются целостными.

---

## Фаза 2: Упростить observability

Стек Prometheus/Grafana/Jaeger/OpenTelemetry уже удален из зависимостей. Осталось упростить код в `internal/observability/`.

### 2.1 Упростить пакет observability 🔄 ЧАСТИЧНО ВЫПОЛНЕНО
- [x] Убрать `BusinessLogger` с методами `LogTransactionEvent`, `LogBudgetEvent` и т.д.
- [x] Оставить базовый structured logger (slog)
- [x] Оставить HTTP logging middleware (`middleware.go`)
- [ ] Упростить health check до одного endpoint'а `/health` — убрать `ReadinessHandler` и `LivenessHandler` из `internal/observability/health.go`

**Статус:** BusinessLogger удалён. Остались `ReadinessHandler` (строка 105) и `LivenessHandler` (строка 138) в `health.go` — не нужны без Kubernetes.

### 2.2 Удалить каталог monitoring/ ✅ ВЫПОЛНЕНО
- [x] Удалить `monitoring/` целиком (Grafana dashboards, Prometheus config, alertmanager, postgres_exporter)

**Статус:** Каталог `monitoring/` удалён.

---

## Фаза 3: Обновить тесты ✅ В ОСНОВНОМ ВЫПОЛНЕНО

### 3.1 Обновить unit-тесты ✅ ВЫПОЛНЕНО
- [x] Обновить тесты репозиториев — убрать `familyID` из вызовов
- [x] Обновить тесты хэндлеров — убрать `family_id` из запросов
- [x] Обновить тесты сервисов — убрать `familyID` из вызовов

**Примечание:** Тест-хелперы (`testhelpers/sqlite.go`) по-прежнему используют familyID для создания записей в БД — это ожидаемо, т.к. колонки family_id остаются в схеме.

### 3.2 Обновить интеграционные тесты ✅ ВЫПОЛНЕНО
- [x] Обновить `internal/testhelpers/sqlite.go` — упростить setup (одна семья)
- [x] Обновить `tests/integration/` — убрать мультисемейные сценарии
- [x] Обновить бенчмарки `tests/benchmarks/`

### 3.3 Обновить веб-тесты ⏸️ ОЖИДАЕТ ФАЗУ 1.5
- [ ] Обновить тесты middleware — убрать family из сессии (после удаления FamilyID из SessionData)
- [ ] Обновить `internal/web/models_test.go` — убрать FamilyID из тестовых данных

---

## Фаза 4: Доработки для self-hosted ⏸️ НЕ НАЧАТО

### 4.1 Первоначальная настройка (onboarding)
- [ ] Реализовать wizard первого запуска: создание семьи + первого пользователя (admin)
- [ ] Если в БД нет семьи — перенаправлять на страницу настройки
- [ ] После создания семьи — перенаправлять на login

### 4.2 Управление пользователями
- [ ] Admin может добавлять/удалять участников семьи через веб-интерфейс
- [ ] Регистрация новых пользователей только через invite от admin (без открытой регистрации)

### 4.3 Резервное копирование через интерфейс
- [ ] Добавить endpoint для скачивания файла БД (SQLite backup)
- [ ] Добавить endpoint для загрузки файла БД (restore)
- [ ] Страница в веб-интерфейсе для управления бэкапами

---

## Сводка по статусам

| Фаза | Задача | Статус |
|------|--------|--------|
| 1.1 | Концепция единственной семьи | ✅ Выполнено |
| 1.2 | Интерфейсы репозиториев | ✅ Выполнено |
| 1.3 | Сервисный слой | ✅ Выполнено |
| 1.4 | API хэндлеры | ✅ Выполнено |
| 1.5 | Веб-слой | 🔄 Почти выполнено (осталось удалить неиспользуемые поля) |
| 1.6 | Доменные модели | ✅ Выполнено |
| 1.7 | Миграции БД | ⚠️ Решено по дизайну |
| 2.1 | Observability | 🔄 Частично (убрать ReadinessHandler/LivenessHandler) |
| 2.2 | Удалить monitoring/ | ✅ Выполнено |
| 3.1 | Unit-тесты | ✅ Выполнено |
| 3.2 | Интеграционные тесты | ✅ Выполнено |
| 3.3 | Веб-тесты | ⏸️ Ожидает фазу 1.5 |
| 4.1 | Onboarding | ⏸️ Не начато |
| 4.2 | Управление пользователями | ⏸️ Не начато |
| 4.3 | Бэкапы | ⏸️ Не начато |

---

## Оставшиеся задачи (приоритет)

### Высокий приоритет (мелкие доработки):
1. **Фаза 1.5** — Удалить `FamilyID` и `SessionFamilyKey` из `session.go` и `models.go`
2. **Фаза 2.1** — Удалить `ReadinessHandler`/`LivenessHandler` из `health.go`
3. **Фаза 3.3** — Обновить веб-тесты после 1.5

### Средний приоритет (новые фичи):
4. **Фаза 4.1** — Onboarding wizard
5. **Фаза 4.2** — Invite-система для пользователей
6. **Фаза 4.3** — Бэкап/восстановление через интерфейс
