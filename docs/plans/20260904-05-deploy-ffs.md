# План 05 — Развёртывание на ffs.shatrov.tech

Пятый план перехода на API-only ([spec 005](../specs/005-api-only-redesign.md), решение A-11).
Одна топология: приложение + Caddy в одном compose на домашнем мини-сервере.

## Overview

- `deploy/docker-compose.yml` — единственный compose для прода: `app` + `caddy:2-alpine`,
  Let's Encrypt автоматически, HTTP/3.
- Caddyfile без плагинных директив, без `/static`, с CSP для JSON и HSTS.
- Бэкапы — CLI `backup` того же бинарника по cron хоста; `backup.sh`, systemd-таймер и
  `make sqlite-backup` как отдельные механизмы уходят.
- nginx, certbot, fail2ban, `minimal`/`prod`/`nginx`/`caddy` варианты, native systemd — удаляются.

## Context (from discovery)

- `deploy/caddy/Caddyfile.template:53,68,100` — `rate_limit` (плагин `mholt/caddy-ratelimit`,
  в стоковом образе нет → Caddy не стартует); CSP разрешает `unpkg.com`; `@static`, `@login_post`.
- `deploy/scripts/install.sh:262` копирует `docker-compose.prod.yml`; генерирует
  `SESSION_SECRET`/`CSRF_SECRET` (`lib/common.sh generate_secret`), которых больше нет.
- `upgrade.sh` — git-ref, бэкап, пересборка, health, откат; `uninstall.sh`; `health-check.sh`.
- `deploy/fail2ban/*` читает nginx-логи и ловит `POST /login … 401` — мёртвый после плана 03.
- `Makefile compose-config` валидирует пять compose-файлов; CI (`ci.yml`) вызывает его.
- `BackupService` (`internal/services/backup_service.go`) — `VACUUM INTO`, ретеншен по числу файлов;
  `BACKUP_DIR` появился в плане 02.
- Healthcheck в compose — `wget -O /dev/null http://localhost:8080/health` с комментарием про
  редирект `/login`.

## Development Approach

- **testing approach**: Regular.
- Проверка — `make compose-config`, `docker compose config`, `docker build`, unit-тесты CLI
  `backup`; на живом сервере — вручную (Post-Completion).
- Скрипты — bash, проверять `shellcheck`.

## Testing Strategy

- unit: `cmd/server/backup.go` (парсинг флагов, вызов сервиса через интерфейс).
- `make compose-config` в CI на единственном compose.
- Ручная проверка на сервере — раздел Post-Completion.

## Progress Tracking

- `[x]` по завершении; ➕ новые задачи; ⚠️ блокеры.

## Solution Overview

Caddy терминирует TLS и проксирует всё на `app:8080`, передавая `X-Forwarded-For`; приложение
доверяет только сети compose (`TRUSTED_PROXIES=172.20.0.0/16`, значение совпадает с подсетью в
compose). Лимитер логина — в приложении (план 03), поэтому Caddy ничего не ограничивает.

Бэкап: `family-budget-service backup` внутри контейнера пишет в `/backups` (bind-mount
`./backups`), cron хоста раз в сутки; ретеншен — `BACKUP_KEEP` (по умолчанию 30). Offsite-копия —
`GET /api/v1/backups/{name}/download` с телефона или `scp`.

## Technical Details

- `deploy/docker-compose.yml`: сервисы `app` (build из `..`, `env_file: .env`, volumes
  `./data:/data`, `./backups:/backups`, `user: 1000:1000`, `cap_drop: ALL`, `read_only: true` с
  tmpfs `/tmp`) и `caddy` (`80`, `443`, `443/udp`, volumes `Caddyfile`, `caddy_data`,
  `caddy_config`, `./logs/caddy`), `depends_on: app: condition: service_healthy`. Сеть `internal`
  с явной подсетью `ipam.config.subnet: 172.20.0.0/16` — иначе Docker выдаст случайную, и
  `TRUSTED_PROXIES` не совпадёт: `IPExtractor` откатится на адрес контейнера Caddy, и per-IP
  лимитер из плана 03 будет считать обоих пользователей одним клиентом.
- `.env.example`: `DOMAIN=ffs.shatrov.tech`, `ACME_EMAIL`, `DATABASE_PATH=/data/budget.db`,
  `BACKUP_DIR=/backups`, `TRUSTED_PROXIES=172.20.0.0/16`, `LOG_LEVEL=info`, `ENVIRONMENT=production`.
- Caddyfile: `{$DOMAIN}`; `encode zstd gzip`; заголовки `Strict-Transport-Security max-age=31536000`,
  `Content-Security-Policy "default-src 'none'; frame-ancestors 'none'"`, `X-Content-Type-Options nosniff`,
  `Referrer-Policy no-referrer`, `-Server`; `reverse_proxy app:8080`; JSON-лог с ротацией;
  `/health` без логирования. Без `rate_limit`, `@static`, `@login_post`.
- CLI `backup [--keep N]`. Сейчас ретеншен — константа `maxBackups = 10` (`backup_service.go:18`),
  `cleanupOldBackups` не экспортирован: `NewBackupService` получает `keep int` (константа
  `defaultBackupKeep = 30` — `mnd`), `CreateBackup` применяет его; выход 0/1;
  cron: `0 3 * * * cd /opt/family-budget && docker compose exec -T app /app/family-budget-service backup`.
- `install.sh`: `--domain` (по умолчанию `ffs.shatrov.tech`), `--email`; без генерации секретов;
  копирует `deploy/docker-compose.yml`, `Caddyfile`, `.env`; в конце печатает команду `setup`
  и строку cron. `upgrade.sh`/`uninstall.sh` — под один compose. `health-check.sh` без изменений.

## Implementation Steps

### Task 1: CLI `backup`

**Files:**
- Create: `cmd/server/backup.go`, `cmd/server/backup_test.go`
- Modify: `cmd/server/main.go`, `internal/services/backup_service.go`, `backup_service_test.go`,
  `Makefile`

- [ ] `NewBackupService(db, dbPath, backupDir, keep, logger)`; `BACKUP_KEEP` в конфиге (по умолчанию 30) — тот же параметр использует API из плана 02
- [ ] подкоманда `backup --keep N` на общем `internal.OpenDatabase`; лог результата в slog
- [ ] `Makefile`: `sqlite-backup` → `go run ./cmd/server backup`, `sqlite-restore` оставить (dev-only, `cp`)
- [ ] тесты: успешный бэкап на `t.TempDir()`, ретеншен оставляет N новейших, ошибка открытия БД → exit 1
- [ ] `make test` — зелёный

### Task 2: Единый compose и Caddyfile

**Files:**
- Create: `deploy/docker-compose.yml`, `deploy/caddy/Caddyfile`, `deploy/.env.example`
- Delete: `deploy/docker-compose.{minimal,prod,nginx,caddy}.yml`, `deploy/caddy/Caddyfile.template`,
  `deploy/nginx/**`, `deploy/fail2ban/**`, `deploy/.env.production.example`
- Modify: `Makefile` (`DEPLOY_COMPOSE_FILES`, `compose-config`), `.github/workflows/ci.yml`

- [ ] compose по «Technical Details»; healthcheck `wget -q -O /dev/null http://localhost:8080/health` без комментария про `/login`
- [ ] `TRUSTED_PROXIES=172.20.0.0/16` задан в compose (`environment`) **и** в `deploy/.env.example`, подсеть совпадает с `ipam.config.subnet`; без него корзина per-IP лимитера выключена (план 03, ревью 4) — за Caddy это единственный способ её включить, и старые `deploy/docker-compose.{caddy,nginx,prod}.yml` её не задают
- [ ] Caddyfile по «Technical Details»; проверить `docker run --rm -v $PWD/deploy/caddy/Caddyfile:/etc/caddy/Caddyfile caddy:2-alpine caddy validate --config /etc/caddy/Caddyfile` (с `DOMAIN=localhost`)
- [ ] `compose-config` валидирует `docker/docker-compose.yml` и `deploy/docker-compose.yml`; убрать цикл «стартует без секретов»
- [ ] `docker compose -f deploy/docker-compose.yml config -q` и `caddy validate` — зелёные локально и в CI

### Task 3: Скрипты установки и обновления

**Files:**
- Modify: `deploy/scripts/install.sh`, `upgrade.sh`, `uninstall.sh`, `lib/common.sh`
- Delete: `deploy/scripts/{backup.sh,install-systemd.sh,setup-fail2ban.sh,setup-ssl-nginx.sh,setup-ssl-caddy.sh}`,
  `deploy/systemd/**`

- [ ] `install.sh`: без секретов и выбора топологии; `.env` из `.env.example` с подстановкой `DOMAIN`/`ACME_EMAIL`; firewall 80/443; печать команды `setup` и cron-строки
- [ ] `upgrade.sh`: бэкап через `docker compose exec app … backup`, один compose-файл; `uninstall.sh` — без образов nginx/caddy-вариантов
- [ ] `lib/common.sh`: удалить `generate_secret`, если больше не используется
- [ ] `shellcheck deploy/scripts/*.sh deploy/scripts/lib/*.sh` — без ошибок
- [ ] прогнать `install.sh --dry-run` (добавить флаг, если нет) в контейнере `ubuntu` — путь до `docker compose up` без ошибок

### Task 4: Документация деплоя

**Files:**
- Modify: `deploy/README.md`, `docs/specs/004-deployment-readiness.md`, `README.md`, `CLAUDE.md`

- [ ] `deploy/README.md` переписать: один сценарий (DNS → `install.sh` → `setup` по ssh → второй пользователь через API → cron бэкапа), обновление, откат, восстановление из бэкапа (`docker compose down`, копия файла, `up`), где логи
- [ ] `004-deployment-readiness.md`: примечание, что D-01/D-03 закрыты удалением секретов и единым compose
- [ ] `README.md` «Self-Hosted Deployment» — одна таблица вместо трёх вариантов; `CLAUDE.md` «Docker»/compose-абзацы
- [ ] `docs/backlog.md`: убрать пункт про двуязычные комментарии в скриптах, если скрипты переписаны на одном языке

### Task 5: Verify acceptance criteria

- [ ] `make compose-config`, `caddy validate`, `shellcheck`, `make pre-commit` — зелёные
- [ ] `docker compose -f deploy/docker-compose.yml up` локально с `DOMAIN=localhost` (Caddy выдаст self-signed) → `curl -k https://localhost/health` 200 → `setup` → login по https
- [ ] `docker network inspect` показывает подсеть `172.20.0.0/16`; в логе неудачного логина `ip` — адрес клиента, а не контейнера Caddy
- [ ] `grep -rn 'SESSION_SECRET\|CSRF_SECRET\|nginx\|fail2ban' deploy docker Makefile .github` пуст

### Task 6: [Final] Update documentation

- [ ] `docs/README.md`, `docs/tech_stack.md` («DevOps» — Caddy, один compose)
- [ ] переместить план в `docs/plans/completed/`

## Post-Completion

**На сервере (владелец):**
- A-запись `ffs.shatrov.tech` на внешний IP, проброс 80/443 на мини-сервер.
- `install.sh --domain ffs.shatrov.tech --email …`, затем `docker compose exec app /app/family-budget-service setup …`
  по ssh; создать второго пользователя через `POST /api/v1/users`.
- Cron бэкапа; проверить, что `./backups` пополняется и файл открывается `sqlite3 … 'PRAGMA integrity_check'`.
- Тег `v0.1.0` — первый релиз без веб-интерфейса.
