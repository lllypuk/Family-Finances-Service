# Правила R8. Своих нет: kotlinx.serialization, Retrofit и OkHttp приносят consumer-правила
# в своих артефактах, а рефлексии в коде приложения нет — модели сериализуются
# сгенерированными сериализаторами, интерфейсы Retrofit создаются по аннотациям.
