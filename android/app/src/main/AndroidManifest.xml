<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Клиент работает только онлайн: без этого разрешения первый же запрос отвечает
         отказом системы, а не сетевой ошибкой, и сообщение вводит в заблуждение. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FamilyFinancesApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FamilyFinances">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
